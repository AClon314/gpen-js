# 存储后续工作

- 为各 Blob backend 增加 `stat()` / `list()` 和引用回收能力。
- 增加 Blob 元数据与文件内容的失败恢复策略。
- 增加 IndexedDB、OPFS、Safari 和 WebExtension 的配额/并发测试。
- 增加 KV 的跨标签变更订阅接口。
- 评估大文件的分块传输和断点续传。
- 跨设备同步另行设计版本号、冲突策略和导入导出协议。
